import { createTheme } from "@mui/material";
const theme = createTheme({
  Colors: {
    primary: {
      main: "#82118C",
    },

    secondary: {
      main: "#39FF14",
    },
  },
});

export default theme;
